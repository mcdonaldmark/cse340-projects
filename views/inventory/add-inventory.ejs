<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Add Vehicle</title>
</head>

<body>
    <main>
        <h1>Add New Vehicle</h1>
        <p>All Fields are Required</p>
        <!-- Flash messages -->
        <% if (messages && messages.length> 0) { %>
            <div class="notice">
                <%= messages %>
            </div>
            <% } %>

                <!-- Validation errors -->
                <% if (errors && errors.length> 0) { %>
                    <div class="errors">
                        <% errors.forEach(err=> { %>
                            <p>
                                <%= err.msg %>
                            </p>
                            <% }) %>
                    </div>
                    <% } %>

                        <form id="addInventoryForm" action="/inv/add-inventory" method="post">
                            <label for="classificationList">Classification:</label>
                            <%- classificationList %>

                                <label for="inv_make">Make:</label>
                                <input type="text" name="inv_make" id="inv_make" required
                                    value="<%= vehicle ? vehicle.inv_make : '' %>">

                                <label for="inv_model">Model:</label>
                                <input type="text" name="inv_model" id="inv_model" required
                                    value="<%= vehicle ? vehicle.inv_model : '' %>">

                                <label for="inv_year">Year:</label>
                                <input type="number" name="inv_year" id="inv_year" required value="<%= vehicle ? vehicle.inv_year : '' %>">

                                <label for="inv_miles">Mileage:</label>
                                <input type="number" name="inv_miles" id="inv_miles" required value="<%= vehicle ? vehicle.inv_miles : '' %>">

                                <label for="inv_description">Description:</label>
                                <textarea name="inv_description" id="inv_description"
                                    required><%= vehicle ? vehicle.inv_description : '' %></textarea>

                                <label for="inv_image">Image Path:</label>
                                <input type="text" name="inv_image" id="inv_image"
                                    value="<%= vehicle ? vehicle.inv_image : '/images/no-image.png' %>">

                                <label for="inv_thumbnail">Thumbnail Path:</label>
                                <input type="text" name="inv_thumbnail" id="inv_thumbnail"
                                    value="<%= vehicle ? vehicle.inv_thumbnail : '/images/no-image.png' %>">

                                <label for="inv_price">Price:</label>
                                <input type="number" name="inv_price" id="inv_price" step="0.01" required
                                    value="<%= vehicle ? vehicle.inv_price : '' %>">

                                <label for="inv_color">Color:</label>
                                <input type="text" name="inv_color" id="inv_color" required
                                    value="<%= vehicle ? vehicle.inv_color : '' %>">

                                <button type="submit">Add Vehicle</button>
                        </form>
    </main>
</body>

</html>